<Window x:Class="Orvian.Browser.PasswordVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orvian Passwort-Tresor" Width="900" Height="620" MinWidth="760" MinHeight="520"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Margin="4,0,4,20"><TextBlock Text="🔐 Passwort-Tresor" FontSize="30" FontWeight="Bold"/><TextBlock Text="Deine Zugangsdaten bleiben auf diesem Windows-Konto lokal verschlüsselt." Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0"/></StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="1.15*"/><ColumnDefinition Width="0.85*"/></Grid.ColumnDefinitions>
            <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="20" Padding="18" Margin="0,0,10,0" Effect="{StaticResource CardShadow}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top"><TextBlock Text="Gespeicherte Logins" FontSize="17" FontWeight="SemiBold"/><TextBlock x:Name="CountText" Text="0 Einträge" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,12"/></StackPanel>
                    <ListBox x:Name="CredentialList" BorderThickness="0" SelectionChanged="CredentialList_SelectionChanged"><ListBox.ItemTemplate><DataTemplate><Border Padding="12" Margin="0,0,0,7" Background="#F7F9FC" CornerRadius="13"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Site}" FontWeight="SemiBold"/><TextBlock Text="{Binding Username}" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,3,0,0"/></StackPanel><TextBlock Grid.Column="1" Text="›" FontSize="20" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/></Grid></Border></DataTemplate></ListBox.ItemTemplate></ListBox>
                </DockPanel>
            </Border>
            <Border Grid.Column="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="20" Padding="20" Margin="10,0,0,0" Effect="{StaticResource CardShadow}">
                <StackPanel><TextBlock Text="Login speichern" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,15"/><TextBlock Text="Website" FontSize="12" Foreground="{StaticResource MutedBrush}"/><TextBox x:Name="SiteBox" Height="38" Padding="10" Margin="0,5,0,12"/><TextBlock Text="Benutzername" FontSize="12" Foreground="{StaticResource MutedBrush}"/><TextBox x:Name="UsernameBox" Height="38" Padding="10" Margin="0,5,0,12"/><TextBlock Text="Passwort" FontSize="12" Foreground="{StaticResource MutedBrush}"/><PasswordBox x:Name="PasswordBox" Height="38" Padding="10" Margin="0,5,0,15"/><StackPanel Orientation="Horizontal"><Button Content="Speichern" Width="118" Height="40" Style="{StaticResource PrimaryButton}" Click="Save_Click"/><Button Content="Löschen" Width="100" Height="40" Margin="8,0,0,0" Click="Delete_Click"/></StackPanel><Border Background="{StaticResource AccentSoftBrush}" Padding="12" CornerRadius="13" Margin="0,18,0,0"><TextBlock Text="🛡 Der Tresor verwendet Windows DPAPI (aktueller Benutzer). Der Inhalt liegt verschlüsselt als lokale Datei vor." TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="12"/></Border></StackPanel>
            </Border>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0"><Button Content="Schließen" Padding="18,9" Click="Close_Click"/></StackPanel>
    </Grid>
</Window>
