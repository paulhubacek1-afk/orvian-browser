<Window x:Class="Orvian.Browser.PasswordVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orvian Passwort-Tresor" Width="760" Height="620" MinWidth="620" MinHeight="480"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Margin="2,0,0,18">
            <TextBlock Text="PASSWORT-TRESOR" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
            <TextBlock Text="Deine Zugangsdaten. Lokal. Verschlüsselt." FontSize="28" FontWeight="Bold" Margin="0,4,0,0"/>
            <TextBlock Text="Orvian schützt den Tresor mit Windows DPAPI für deinen Benutzer." Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0"/>
        </StackPanel>
        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="18" Padding="12">
            <Grid>
                <ListBox x:Name="CredentialList" BorderThickness="0" Background="Transparent" />
                <TextBlock x:Name="EmptyText" Text="Noch keine Zugangsdaten gespeichert." Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15"/>
            </Grid>
        </Border>
        <DockPanel Grid.Row="2" Margin="0,16,0,0" LastChildFill="False">
            <Button Content="＋ Zugangsdaten hinzufügen" Style="{StaticResource PrimaryButton}" Width="205" Height="42" Click="Add_Click" DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Löschen" Width="90" Height="42" Margin="8,0,8,0" Click="Delete_Click"/>
                <Button Content="Schließen" Width="95" Height="42" Click="Close_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>