<Window x:Class="Orvian.Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orvian Browser"
        Width="1500"
        Height="920"
        MinWidth="900"
        MinHeight="620"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Storyboard x:Key="Intro">
            <DoubleAnimation Storyboard.TargetName="Chrome" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.16"/>
            <DoubleAnimation Storyboard.TargetName="Page" Storyboard.TargetProperty="Opacity" From="0" To="1" BeginTime="0:0:0.03" Duration="0:0:0.2"/>
        </Storyboard>
        <Storyboard x:Key="WelcomeIntro">
            <DoubleAnimation Storyboard.TargetName="WelcomeOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.16"/>
            <DoubleAnimation Storyboard.TargetName="WelcomeCardScale" Storyboard.TargetProperty="ScaleX" From="0.96" To="1" Duration="0:0:0.26"/>
            <DoubleAnimation Storyboard.TargetName="WelcomeCardScale" Storyboard.TargetProperty="ScaleY" From="0.96" To="1" Duration="0:0:0.26"/>
        </Storyboard>
        <Storyboard x:Key="WelcomeOutro">
            <DoubleAnimation Storyboard.TargetName="WelcomeOverlay" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.14"/>
            <DoubleAnimation Storyboard.TargetName="WelcomeCardScale" Storyboard.TargetProperty="ScaleX" From="1" To="0.98" Duration="0:0:0.14"/>
            <DoubleAnimation Storyboard.TargetName="WelcomeCardScale" Storyboard.TargetProperty="ScaleY" From="1" To="0.98" Duration="0:0:0.14"/>
        </Storyboard>
        <Storyboard x:Key="CreatorIntro">
            <DoubleAnimation Storyboard.TargetName="CreatorOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.18"/>
            <DoubleAnimation Storyboard.TargetName="CreatorCardScale" Storyboard.TargetProperty="ScaleX" From="0.98" To="1" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="CreatorCardScale" Storyboard.TargetProperty="ScaleY" From="0.98" To="1" Duration="0:0:0.2"/>
        </Storyboard>
        <Storyboard x:Key="CreatorOutro">
            <DoubleAnimation Storyboard.TargetName="CreatorOverlay" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.12"/>
        </Storyboard>
        <Storyboard x:Key="UpdateIntro">
            <DoubleAnimation Storyboard.TargetName="UpdateOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.16"/>
        </Storyboard>

        <LinearGradientBrush x:Key="ToolbarGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#F8FAFD" Offset="1"/>
        </LinearGradientBrush>
        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="#EEF2F7"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.72"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Root" Property="Opacity" Value="0.35"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TabButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="MaxWidth" Value="240"/>
            <Setter Property="Margin" Value="3,5,2,3"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#E8EDF4"/>
            <Setter Property="Foreground" Value="#56647A"/>
            <Setter Property="BorderBrush" Value="#D8E0EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="11">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Background" Value="#F3F6FA"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.82"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RoundPrimary" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="21" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.92"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Root" Property="Opacity" Value="0.78"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Root" Property="Opacity" Value="0.45"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="62"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="Chrome" Grid.Row="0" Background="{StaticResource TabBarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Opacity="0">
            <Grid Margin="8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="32" Height="32" CornerRadius="10" Background="{StaticResource AccentBrush}" Margin="0,0,8,0">
                        <TextBlock Text="O" FontSize="17" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Orvian" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden" Margin="12,0,10,0">
                    <StackPanel x:Name="TabStrip" Orientation="Horizontal" VerticalAlignment="Center"/>
                </ScrollViewer>
                <Button x:Name="NewTabButton" Grid.Column="2" Content="＋" Width="38" Height="36" FontSize="19" Click="NewTab_Click" ToolTip="Neuer Tab (Ctrl+T)" Style="{StaticResource ChromeButton}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource ToolbarGradient}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BackButton" Content="‹" Style="{StaticResource ChromeButton}" Click="Back_Click" ToolTip="Zurück"/>
                    <Button x:Name="ForwardButton" Content="›" Style="{StaticResource ChromeButton}" Click="Forward_Click" ToolTip="Vorwärts"/>
                    <Button Content="↻" Style="{StaticResource ChromeButton}" Click="Reload_Click" ToolTip="Neu laden"/>
                    <Button Content="⌂" Style="{StaticResource ChromeButton}" Click="Home_Click" ToolTip="Startseite"/>
                </StackPanel>

                <Border Grid.Column="2" Height="44" Background="#FFFFFF" BorderBrush="#CDD7E3" BorderThickness="1" CornerRadius="22" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                    <Grid Margin="6,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="38"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="38"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="28" Height="28" CornerRadius="14" Background="#EEF2F7" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="⌕" FontSize="17" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBox x:Name="AddressBox" Grid.Column="1" BorderThickness="0" Background="Transparent" FontSize="14" Padding="0" VerticalContentAlignment="Center" KeyDown="AddressBox_KeyDown"/>
                        <Button Grid.Column="2" Content="☆" Width="32" Height="32" Foreground="{StaticResource MutedBrush}" Click="Bookmark_Click" ToolTip="Lesezeichen"/>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                    <Button x:Name="PrivacyButton" Content="◈" Style="{StaticResource ChromeButton}" Foreground="{StaticResource AccentBrush}" Click="Privacy_Click" ToolTip="Datenschutz-Center"/>
                    <Button Content="⋮" Style="{StaticResource ChromeButton}" FontSize="20" Click="Menu_Click" ToolTip="Menü und Einstellungen"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid x:Name="Page" Grid.Row="2" Background="#F7F9FC" Opacity="0">
            <ContentControl x:Name="BrowserHost"/>
            <Grid x:Name="PandaLayer" IsHitTestVisible="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Panel.ZIndex="50"/>

            <Border x:Name="LockOverlay" Background="#CCF5F7FA" Visibility="Collapsed">
                <Border Width="430" Padding="32" Background="#FFFFFF" BorderBrush="#D8E0EA" BorderThickness="1" CornerRadius="24" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="Orvian ist gesperrt" FontSize="27" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Alt + D zum Entsperren" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" Margin="0,8,0,20"/>
                        <PasswordBox x:Name="UnlockBox" Height="42" Padding="12"/>
                        <Button Content="Entsperren" Margin="0,12,0,0" Click="Unlock_Click" Style="{StaticResource RoundPrimary}"/>
                    </StackPanel>
                </Border>
            </Border>

            <Border x:Name="WelcomeOverlay" Background="#D9F3F6FA" Visibility="Collapsed" Opacity="0">
                <Border Width="820" MaxWidth="900" Padding="36" Background="#FFFFFF" BorderBrush="#D7E0EA" BorderThickness="1" CornerRadius="28" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                    <Border.RenderTransform>
                        <ScaleTransform x:Name="WelcomeCardScale" CenterX="410" CenterY="240"/>
                    </Border.RenderTransform>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="190"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Border Width="138" Height="138" CornerRadius="45" Background="#EEF3FF">
                                <TextBlock Text="🐼" FontSize="88" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="ORVIAN" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,13,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                            <TextBlock Text="Willkommen bei Orvian" FontSize="34" FontWeight="Bold"/>
                            <TextBlock Text="Schnell. Privat. Persönlich." FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" Margin="0,5,0,12"/>
                            <TextBlock Text="Ein moderner Chromium-basierter Browser mit echten Tabs, Datenschutz-Funktionen, Passwort-Tresor und dem Orvian-Panda." TextWrapping="Wrap" FontSize="14" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,18"/>
                            <WrapPanel Margin="0,0,0,18">
                                <Border Background="#F2F5F9" CornerRadius="10" Padding="10,7" Margin="0,0,6,6"><TextBlock Text="Echte Tabs" FontWeight="SemiBold"/></Border>
                                <Border Background="#F2F5F9" CornerRadius="10" Padding="10,7" Margin="0,0,6,6"><TextBlock Text="Chromium / WebView2" FontWeight="SemiBold"/></Border>
                                <Border Background="#F2F5F9" CornerRadius="10" Padding="10,7" Margin="0,0,6,6"><TextBlock Text="Datenschutz" FontWeight="SemiBold"/></Border>
                                <Border Background="#F2F5F9" CornerRadius="10" Padding="10,7" Margin="0,0,6,6"><TextBlock Text="Passwort-Tresor" FontWeight="SemiBold"/></Border>
                            </WrapPanel>
                            <Button Content="Los geht's →" Width="160" HorizontalAlignment="Left" Click="WelcomeContinue_Click" Style="{StaticResource RoundPrimary}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>

            <Border x:Name="CreatorOverlay" Background="#D9F3F6FA" Visibility="Collapsed" Opacity="0">
                <Border Width="650" Padding="32" Background="#FFFFFF" BorderBrush="#D7E0EA" BorderThickness="1" CornerRadius="24" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                    <Border.RenderTransform>
                        <ScaleTransform x:Name="CreatorCardScale" CenterX="325" CenterY="205"/>
                    </Border.RenderTransform>
                    <StackPanel>
                        <TextBlock Text="Creator-Support" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Unterstütze Noctra11" FontSize="28" FontWeight="Bold" Margin="0,4,0,0"/>
                        <TextBlock Text="Wenn dir die Inhalte gefallen, kannst du den Kanal direkt auf Twitch oder YouTube unterstützen." TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,22"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Twitch" Width="140" Margin="0,0,10,0" Click="CreatorLink_Click" Tag="https://www.twitch.tv/noctra11" Style="{StaticResource RoundPrimary}"/>
                            <Button Content="YouTube" Width="140" Margin="0,0,10,0" Click="CreatorLink_Click" Tag="https://www.youtube.com/@Noctra11_Yt" Style="{StaticResource RoundPrimary}"/>
                            <Button Content="Später" Width="95" Click="CreatorBannerLater_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>

            <Border x:Name="UpdateOverlay" Background="#D9F3F6FA" Visibility="Collapsed" Opacity="0">
                <Border Width="570" Padding="32" Background="#FFFFFF" BorderBrush="#D7E0EA" BorderThickness="1" CornerRadius="24" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="Orvian-Update" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Ein neues Update ist verfügbar" FontSize="27" FontWeight="Bold" Margin="0,5,0,0"/>
                        <TextBlock x:Name="UpdateText" Text="Eine neue Version wurde gefunden." TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,22"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Später" Margin="0,0,8,0" Padding="16,9" Click="UpdateLater_Click"/>
                            <Button x:Name="UpdateNowButton" Content="Jetzt aktualisieren" Padding="16,9" Click="UpdateNow_Click" Style="{StaticResource RoundPrimary}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Grid>
</Window>