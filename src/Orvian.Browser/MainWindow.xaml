<Window x:Class="Orvian.Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orvian Browser" Width="1280" Height="800" MinWidth="900" MinHeight="600"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10,10,10,6">
            <TextBlock Text="◈ ORVIAN" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,14,0"/>
            <Button x:Name="BackButton" Content="‹" Width="38" Click="Back_Click"/>
            <Button x:Name="ForwardButton" Content="›" Width="38" Click="Forward_Click" Margin="4,0,0,0"/>
            <Button Content="↻" Width="38" Click="Reload_Click" Margin="4,0,0,0"/>
            <TextBox x:Name="AddressBox" Margin="8,0,8,0" Height="34" VerticalContentAlignment="Center"
                     Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}"
                     BorderThickness="0" Padding="12,0" KeyDown="AddressBox_KeyDown"/>
            <Button Content="☰" Width="42" Click="Menu_Click"/>
        </DockPanel>

        <Border Grid.Row="1" Margin="10,0,10,8" Background="{StaticResource SurfaceBrush}" CornerRadius="10" Padding="8">
            <DockPanel>
                <TextBlock x:Name="PrivacyStats" Text="Orvian • Schutz aktiv" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}"/>
                <Button DockPanel.Dock="Right" Content="Neue App" Click="InstallApp_Click" Margin="8,0,0,0"/>
                <Button DockPanel.Dock="Right" Content="Passwort prüfen" Click="CheckPassword_Click" Margin="8,0,0,0"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2" Margin="10,0,10,10">
            <wv2:WebView2 x:Name="BrowserView" xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"/>
            <Border x:Name="LockOverlay" Background="#EE080B10" Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="360">
                    <TextBlock Text="ORVIAN GESPERRT" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Alt+D zum Entsperren" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,20" HorizontalAlignment="Center"/>
                    <PasswordBox x:Name="UnlockBox" Height="40" Padding="10"/>
                    <Button Content="Entsperren" Height="40" Margin="0,10,0,0" Click="Unlock_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>